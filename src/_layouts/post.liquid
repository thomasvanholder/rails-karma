---
layout: default
---

<div class="flex max-w-5xl mx-auto mb-10 lg:max-w-6xl xl:max-w-7xl">
  <div class="hidden pt-8 lg:block lg:w-1/4 xl:w-1/5">
		<div class="sticky flex flex-col w-full h-auto p-2 text-purple-700 bg-white border-4 border-gray-500 rounded-lg cursor-pointer top-16">
      {% for item in page.navigation %}
        {% assign item_regexed = item | replace: " ", "-"  | downcase | remove: "." | remove: "?" %}
          <a class="py-3" href="#{{ item_regexed }}" data-controller="scroll-to">{{item}}</a>
      {% endfor %}
		</div>
	</div>

<div markdown='1' class="max-w-3xl p-8 mx-auto lg:max-w-4xl lg:w-4/6 xl:w-4/5">
  <article markdown='1' class="prose prose-md lg:prose-lg max-w-none">

  {{ content }}

  </article>
</div>
</div>
